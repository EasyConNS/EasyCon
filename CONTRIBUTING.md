# 贡献指南

感谢关注伊机控（EasyCon）！为了让大家的精力都花在正事上、避免观念分歧导致的无用功，参与贡献前请先了解以下三条核心原则：

1. **功能与架构类变更：Issue 先行** —— 先讨论达成共识，再动手实现；
2. **不涉及核心功能与架构的小改动：直接提 PR** —— bug 修复、小幅改进随时欢迎；
3. **人工审核不可省略** —— PR 由人负责，不能全由 Agent 决策。

## 功能与架构类变更：先讨论，后动手

功能更新往往涉及产品观念与取舍，天然容易产生分歧。以下改动请**先开 Issue（使用「功能提案」模板）讨论，与维护者达成共识后再实现**：

- 新功能，或现有功能的行为变化；
- 涉及核心功能与架构的调整，例如：脚本语言与编译器、VM 指令集（`src/EasyCon.Script`、`src/EasyCon.Vm`）、核心抽象与模块划分（`src/EasyCon.Core`）、虚拟手柄与设备通信、图像识别管线；
- 会影响脚本作者可见行为的改动（`docs/Script.md`、`docs/Functions.md` 所覆盖的内容）。

未经讨论直接提交的此类 PR 可能不会被合并——即使代码本身没有问题，因为双方对「应该做什么」还没有共识。

推荐流程：开功能提案 Issue → 讨论并确认方案 → 动手实现 → 提 PR 并关联该 Issue。

## 小改动：直接提 PR

以下改动无需预先开 Issue，直接提 PR 即可，随时欢迎：

- Bug 修复；
- 不涉及核心功能与架构、改动幅度较小且合理的改进（例如近期的中英双语切换）；
- 文档、示例脚本、翻译完善。

如果不确定自己的改动算不算「核心功能 / 架构」，先开个 Issue 问一句——问一句的成本远低于返工。

## 人工审核不可省略

允许并欢迎使用 AI / Agent 辅助开发，但有一点必须明确：

- 提交前，贡献者必须**本人逐行审核全部代码**（包括 AI 生成的部分），理解并认可每一处改动；
- PR 提交者对内容负全部责任。不接受全由 Agent 决策产出、提交者自己没有审核过（或解释不了）的 PR，这类 PR 通常会被直接关闭；
- 希望贡献者具备一定的开发经验。你比任何工具都更了解你的需求，也只有你能对最终提交负责。

## 提交前自查

- [ ] 已逐行人工审核全部代码（含 AI 生成部分），能解释每一处改动的原因；
- [ ] `dotnet build` 通过；
- [ ] `dotnet test EasyCon2.slnx -c Release` 通过；
- [ ] `dotnet format --verify-no-changes` 无差异（CI 格式门禁，阻塞合并）；
- [ ] 新功能 / 修复附带 NUnit 测试（本项目用 `[Test]`，不用 xUnit / MSTest）；
- [ ] 涉及脚本语言 / VM / 架构的改动已同步更新 `docs/` 下相关文档。

## 代码与提交约定

- 遵循根目录 `.editorconfig`：4 空格缩进、CRLF、文件末尾无换行、`_camelCase` 私有字段、显式类型优先等，以现有代码风格为准；
- 功能 PR 默认基于并提交到 `dev` 分支，`main` 用于发布；
- PR 描述请说清楚：改了什么、为什么改、如何验证。